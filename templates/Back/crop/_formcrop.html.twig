{{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
<div>
    <label class="block text-sm font-medium text-gray-700 mb-1">
        Crop Name
    </label>
    {{ form_widget(form.name, {
        'attr': {
            'class': 'w-full p-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-[#22C55E] outline-none'
        }
    }) }}
</div>

<button type="submit"
        class="bg-[#22C55E] hover:bg-[#16A34A] text-white px-6 py-3 rounded-lg font-medium">
    Save Crop
</button>

{{ form_end(form) }}